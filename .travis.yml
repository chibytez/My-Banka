language: node_js
node_js:
  - "10.8.0"
install:
  - npm install

  script:
  -npm run coverage


after_script:
   - npm run coverage
   - ./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT
